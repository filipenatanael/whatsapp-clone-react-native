const INITIAL_STATE = {}
//console.log('Testing: ', action.payload);
export default (state = INITIAL_STATE, action) => {
  //console.log('Testing: ', action.payload);
  switch (action.type) {
    default:
      return state;
  }
}
